

    interface objectTodos{

            id: number,
            names: string,
            isDane: boolean,
            showEdeting:boolean,
            completed: boolean|string,
            completedCount:number,
            confirmed?: boolean
        }

    export default objectTodos;